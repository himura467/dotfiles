## Installation

Run this:

```sh
git clone https://github.com/himura467/dotfiles.git ~/.dotfiles && cd ~/.dotfiles && git config filter.ghostty-theme.clean "sed 's/^theme = .*/theme = YOUR_GHOSTTY_THEME_HERE/'" && git submodule update --init && ./bootstrap.sh
```
